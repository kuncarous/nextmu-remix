{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: dev",
            "request": "launch",
            "command": "pnpm run dev",
            "cwd": "${workspaceFolder}",
            "sudo": true
        }
    ]
}